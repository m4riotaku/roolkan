export interface Book {
  id: string;
  title: string;
  demography: string;
  genre: string;
  status: string;
  // NOTE:  campos fuera de capa
  //image: string;
  //url: string;
  // raiting: number;
}
